# Keyword Group Set Versioning

Scripts-first, UI later.

## Rules
- Create a new group set for each strategy change.
- Do not overwrite or delete old sets.
- Use `is_active` to mark the current set.
- Group names must match CSV headers exactly.
