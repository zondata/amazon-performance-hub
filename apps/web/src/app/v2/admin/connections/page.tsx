import V2PlaceholderPage from '../../_components/V2PlaceholderPage';

export default function V2AdminConnectionsPage() {
  return (
    <V2PlaceholderPage
      title="V2 Admin Connections Placeholder"
      routePath="/v2/admin/connections"
      summary="This route reserves the future V2 connections admin surface. Auth implementation is intentionally deferred to a later bounded task."
    />
  );
}
